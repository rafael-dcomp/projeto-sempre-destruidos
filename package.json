{
  "dependencies": {
    "express": "^5.2.1",
    "socket.io": "^4.8.1"
  },
  "name": "jogomultplayersoccer",
  "version": "1.0.0",
  "main": "dist/game-server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && tsc -p tsconfig.client.json",
    "build:server": "tsc",
    "build:client": "tsc -p tsconfig.client.json",
    "start": "npm run build && node dist/game-server.js",
    "dev": "ts-node game-server.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.0.3",
    "@types/socket.io": "^3.0.1",
    "@types/socket.io-client": "^1.4.36",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
